"""
Package ingestion — pipeline d'ingestion des données normalisées vers Graphiti
(graphe de connaissances temporel sur Neo4j).

Assure l'idempotence : réexécuter une ingestion ne duplique pas les nœuds.
"""

